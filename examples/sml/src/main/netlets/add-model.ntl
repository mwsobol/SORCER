// model three entries

   Double v1 = 100.0;
   Double v2 = 200.0;

  Model context = model(inEnt("x1", v1), inEnt("x2", v2),
                outEnt("result/y", invoker("x1 + x2", ents("x1", "x2"))));

  responseUp(context, "result/y", "x1");
