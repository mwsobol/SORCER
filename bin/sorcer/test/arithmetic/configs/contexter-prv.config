/* 
* Provider dependency injections
* It uses component entry names defined in the SORCER provider
* as defined by sorcer.core.provider.SorcerProvider.
*/
import net.jini.jrmp.*;
import net.jini.jeri.*;
import net.jini.jeri.tcp.*;
import net.jini.core.entry.Entry;
import net.jini.lookup.entry.*;
import sorcer.core.*;
import sorcer.service.*;
import junit.sorcer.core.provider.ArithmeticNetTest;

sorcer.core.provider.ServiceExerter {
        /* service provider deployment properties */
		name = "Add Contexter";
		description = "Provides the context for arithmetic operations";
		location = "AFRL";
		publishedInterfaces = new Class[] { sorcer.service.Contextion.class, sorcer.service.Invocation.class, sorcer.service.Evaluation.class };
			
		// service beans	
		beans = new Object[] { junit.sorcer.core.provider.ArithmeticNetTest.createContext() }; 
		
		iconName="sorcer.jpg";
}
	



