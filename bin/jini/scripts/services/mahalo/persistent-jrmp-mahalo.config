import net.jini.jrmp.JrmpExporter;

com.sun.jini.mahalo {
    initialLookupGroups = new String[] { "${INITIAL_LOOKUP_GROUP}" };
    persistenceDirectory = "${LOG_DIR}/mahalo.log";
    serverExporter = new JrmpExporter();
}
