version = sorcerVersion

jar {
	manifest {
		attributes("Implementation-Title": "SORCER Exertion Execution Monitoring Service", "Implementation-Version": version)
	   }
}

dependencies { compile project(':sorcer-dl'), project(':sorcer-platform') }
