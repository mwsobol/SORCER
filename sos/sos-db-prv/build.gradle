version = sorcerVersion

jar {
	manifest {
		attributes("Implementation-Title": "SORCER Berkeley Database Service", "Implementation-Version": version)
	   }
}

dependencies { compile project(':sorcer-platform') }
